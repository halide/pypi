name: Build FlatBuffers wheels

on:
  push:
    branches: [main]
    paths:
      - packages/flatbuffers/**
  workflow_dispatch: {}

jobs:
  validate:
    uses: ./.github/workflows/validate.yml
    permissions:
      contents: read

  build:
    needs: validate
    uses: ./.github/workflows/build-dependency.yml
    with:
      package: packages/flatbuffers
    permissions:
      contents: write
      pages: write
      id-token: write
